export const metadata = {
   title: 'Tambah Pasien',
   description: 'Welcome to Next.js',
};

import React from 'react';
import ManagePatientPage from './[id]/page';

const page = () => {
   return (
      <ManagePatientPage />
   );
};

export default page;