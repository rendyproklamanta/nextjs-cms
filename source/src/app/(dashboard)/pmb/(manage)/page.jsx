import React from 'react';
import ManagePmbPage from './[id]/page';

export const metadata = {
   title: 'Tambah PMB',
   description: 'Welcome to Next.js',
};

const page = () => {
   return (
      <ManagePmbPage />
   );
};

export default page;