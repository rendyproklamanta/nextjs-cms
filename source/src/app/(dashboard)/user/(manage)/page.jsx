import React from 'react';
import ManageUserPage from '../../../../components/form/UserForm';

export const metadata = {
   title: 'Tambah User',
   description: 'Welcome to Next.js',
};

const page = () => {
   return <ManageUserPage />;
};

export default page;
